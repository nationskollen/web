/*
import React from 'react';
import { Story, Meta } from '@storybook/react';

import ModalForm, { Props } from './ModalForm';

export default {
  title: 'common/ModalForm',
  component: ModalForm,
} as Meta;

const Template: Story<Props> = (args) => <ModalForm {...args} />;

export const Primary = Template.bind({});
Primary.args = {
    title: 'title'
};
*/
