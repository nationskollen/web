/*
import React from 'react';
import { Story, Meta } from '@storybook/react';

import PaginationActions, { Props } from './PaginationActions';

export default {
  title: 'common/PaginationActions',
  component: PaginationActions,
} as Meta;

const Template: Story<Props> = (args) => <PaginationActions {...args} />;

export const Primary = Template.bind({});
Primary.args = {
    page: 1
};
*/
