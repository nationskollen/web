/*
import React from 'react';
import { Story, Meta } from '@storybook/react';

import ModalSteps, { Props } from './ModalSteps';

export default {
  title: 'common/ModalSteps',
  component: ModalSteps,
} as Meta;

const Template: Story<Props> = (args) => <ModalSteps {...args} />;

export const Primary = Template.bind({});
Primary.args = {
    title: 'title'
};
*/
